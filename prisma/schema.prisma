generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model NamedLocation {
  id   String @id @default(cuid())
  name String
  lat  Float
  lng  Float

  @@map("named_locations")
}
